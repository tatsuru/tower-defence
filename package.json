{
  "name": "tower-defense",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name tower-defence"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.2",
    "wrangler": "^4.83.0"
  },
  "dependencies": {
    "phaser": "^3.88.2"
  }
}
